import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { spawnSync } from 'node:child_process';

const __dirname=path.dirname(fileURLToPath(import.meta.url));
const MARKER='UI_STABILITY_V2617';
function must(...p){const f=path.join(__dirname,...p);if(!fs.existsSync(f))throw new Error(`[v2617] missing ${p.join('/')}`);return f}
function check(f){const r=spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0)throw new Error(`[v2617] syntax invalid ${path.basename(f)}: ${String(r.stderr||r.stdout||'').trim()}`)}
function save(f,b,a){if(a===b)return false;const ext=path.extname(f)||'.tmp',tmp=`${f}.v2617-${process.pid}-${Date.now()}${ext}`;fs.writeFileSync(tmp,a,'utf8');try{if(ext==='.js'||ext==='.mjs')check(tmp);fs.renameSync(tmp,f)}catch(e){try{fs.unlinkSync(tmp)}catch{};throw e}return true}
function replaceRange(s,startNeedle,endNeedle,replacement,label){const a=s.indexOf(startNeedle),b=a>=0?s.indexOf(endNeedle,a+startNeedle.length):-1;if(a<0||b<0)throw new Error(`[v2617] ${label} anchor missing`);return s.slice(0,a)+replacement+s.slice(b)}

function patchApp(){
  const f=must('public','app.js'),before=fs.readFileSync(f,'utf8');let s=before;if(s.includes(MARKER))return false;
  const anchor="const $=id=>document.getElementById(id);";
  if(!s.includes(anchor))throw new Error('[v2617] app root anchor missing');
  const helper=`${anchor}\n\n// ${MARKER}: one scroll owner, no repeated correction loops.\nconst UI_MOTION_IDLE_MS_V2617=1100;\nlet uiMotionUntilV2617=0,tvReturnBusyV2617=false,tvReturnSettleTimerV2617=null,lastRankSigV2617='',lastTestSigV2617='';\nfunction markUiMotionV2617(ms=UI_MOTION_IDLE_MS_V2617){uiMotionUntilV2617=Math.max(uiMotionUntilV2617,Date.now()+ms)}\nfunction uiMotionHeldV2617(){return Date.now()<uiMotionUntilV2617||tvReturnBusyV2617||Number(window.__tvReturnHoldUntilV2617||0)>Date.now()}\nfunction uiAutoRefreshAllowedV2617(){return !document.hidden&&!uiMotionHeldV2617()}\nwindow.uiAutoRefreshAllowedV2617=uiAutoRefreshAllowedV2617;window.uiMotionHeldV2617=uiMotionHeldV2617;\nfor(const ev of ['touchstart','touchmove','pointerdown','wheel'])document.addEventListener(ev,()=>markUiMotionV2617(ev==='touchmove'?1300:900),{passive:true,capture:true});\nwindow.addEventListener('scroll',()=>markUiMotionV2617(900),{passive:true});\nfunction stableElementKeyV2617(el){if(!el)return'';const direct=el.dataset?.observationKey||el.dataset?.actualId||el.dataset?.id||el.dataset?.signalKey||'';if(direct)return String(direct);const tv=el.matches?.('[data-tv-symbol]')?el:el.querySelector?.('[data-tv-symbol]');const sym=String(tv?.dataset?.tvSymbol||tv?.textContent||'').toUpperCase().replace(/[^A-Z0-9]/g,'');const dir=/做空/.test(el.textContent||'')?'SHORT':/做多/.test(el.textContent||'')?'LONG':'';return sym?\`TV:\${sym}:\${dir}\`:''}\nfunction captureViewportAnchorV2617(root){if(!root||!root.isConnected||uiMotionHeldV2617())return null;const topLine=Math.max(86,Math.min(150,window.innerHeight*.16)),nodes=[...root.querySelectorAll('.rankCard,.testCard,.testMonitorCard,.manual-card,.actualTradeItemV2613,.manual-shadow-history-row,.abc-sample-row')];let pick=null;for(const el of nodes){const r=el.getBoundingClientRect();if(r.bottom<=topLine||r.top>=window.innerHeight)continue;if(r.top<=topLine){pick=el;break}if(!pick)pick=el}if(!pick)return null;return{key:stableElementKeyV2617(pick),top:pick.getBoundingClientRect().top,scrollY:window.scrollY||0}}\nfunction restoreViewportAnchorV2617(root,a){if(!a||!root||uiMotionHeldV2617())return;requestAnimationFrame(()=>requestAnimationFrame(()=>{if(uiMotionHeldV2617())return;let target=null;if(a.key){for(const el of root.querySelectorAll('.rankCard,.testCard,.testMonitorCard,.manual-card,.actualTradeItemV2613,.manual-shadow-history-row,.abc-sample-row')){if(stableElementKeyV2617(el)===a.key){target=el;break}}}if(target){const delta=target.getBoundingClientRect().top-Number(a.top||0);if(Math.abs(delta)>8&&Math.abs(delta)<window.innerHeight*2.5)window.scrollBy({top:delta,left:0,behavior:'auto'})}}))}\nfunction rankSignatureV2617(d){return JSON.stringify((d?.rows||[]).slice(0,16).map(x=>[x?.symbol,x?.direction,Math.round(Number(x?.rankScore||0)),Math.round(Number(x?.estimatedWinRate||0)*10),String(x?.profile?.purpose||'')]))}\nfunction testSignatureV2617(d){return JSON.stringify((d?.rows||[]).slice(0,32).map(x=>[x?.key||x?.symbol,x?.status,Math.round(Number(x?.observationProgress||0)),x?.notificationTier,Math.round(Number(x?.monitorScore||0)),String(x?.strategyProfile?.id||x?.strategyAtConfirm?.id||'')]))}\n`;
  s=s.replace(anchor,helper);

  // Replace the old 8-12 repeated scroll corrections with a single settle-and-restore pass.
  const cap=`function tvReturnCaptureV268(a){\n  if(!a?.href||!String(a.href).includes('tradingview.com'))return;\n  const card=a.closest('.rankCard,.testCard,.testMonitorCard,.actualTradeMonitorCard,.biasRow,.matrixCoin,.sg-candidate-card,.manual-card,.manual-shadow-history-row,.abc-sample-row,.actualTradeItemV2613'),symbol=String(a.dataset?.tvSymbol||a.textContent||'').toUpperCase().replace(/[^A-Z0-9]/g,''),page=document.querySelector('.pageTab.active')?.dataset?.page||'';\n  const data={at:Date.now(),href:a.href,symbol,page,key:stableElementKeyV2617(card||a),top:a.getBoundingClientRect().top,cardTop:card?.getBoundingClientRect().top??null,scrollY:window.scrollY||document.documentElement.scrollTop||0};\n  window.__tvReturnHoldUntilV2617=Date.now()+7000;try{history.scrollRestoration='manual'}catch{}try{sessionStorage.setItem(TV_RETURN_KEY_V268,JSON.stringify(data))}catch{}\n}\n`;
  s=replaceRange(s,'function tvReturnCaptureV268(a){','\nfunction tvReturnApplyV268(){',cap,'TV capture');
  const apply=`function tvReturnApplyV268(){\n  const d=tvReturnReadV268();if(!d||Date.now()-Number(d.at||0)>15*60_000){tvReturnClearV268();tvReturnBusyV2617=false;return false}\n  const page=String(d.page||'').replace(/[^a-z0-9_-]/gi,'');if(page&&document.querySelector('.pageTab.active')?.dataset?.page!==page){try{setPage(page,{force:true})}catch{document.querySelector(\`.pageTab[data-page="\${page}"]\`)?.click()}return false}\n  const sym=String(d.symbol||''),candidates=[...document.querySelectorAll('.rankCard,.testCard,.testMonitorCard,.actualTradeMonitorCard,.biasRow,.matrixCoin,.sg-candidate-card,.manual-card,.manual-shadow-history-row,.abc-sample-row,.actualTradeItemV2613')].filter(x=>x.offsetParent!==null);let card=null;if(d.key)card=candidates.find(x=>stableElementKeyV2617(x)===d.key)||null;if(!card&&sym)card=candidates.find(x=>String(x.querySelector?.('[data-tv-symbol]')?.dataset?.tvSymbol||'').toUpperCase()===sym)||null;const link=card?.querySelector?.('[data-tv-symbol]')||[...document.querySelectorAll('[data-tv-symbol]')].find(x=>String(x.dataset?.tvSymbol||'').toUpperCase()===sym&&x.offsetParent!==null)||[...document.querySelectorAll('a[href]')].find(x=>x.href===d.href&&x.offsetParent!==null);\n  let done=false;if(card&&Number.isFinite(Number(d.cardTop))){const delta=card.getBoundingClientRect().top-Number(d.cardTop);if(Math.abs(delta)<window.innerHeight*3){window.scrollBy({top:delta,left:0,behavior:'auto'});done=true}}if(!done&&link){const delta=link.getBoundingClientRect().top-Number(d.top||0);if(Math.abs(delta)<window.innerHeight*3){window.scrollBy({top:delta,left:0,behavior:'auto'});done=true}}if(!done)window.scrollTo({top:Number(d.scrollY||0),left:0,behavior:'auto'});return true\n}\n`;
  s=replaceRange(s,'function tvReturnApplyV268(){','\nfunction tvReturnRestoreV268(){',apply,'TV apply');
  const restore=`function tvReturnRestoreV268(){
  const d=tvReturnReadV268();if(!d||Date.now()-Number(d.at||0)>15*60_000||tvReturnBusyV2617)return;
  tvReturnBusyV2617=true;window.__tvReturnHoldUntilV2617=Date.now()+8000;markUiMotionV2617(8000);for(const t of tvReturnTimersV268)clearTimeout(t);tvReturnTimersV268=[];if(tvReturnSettleTimerV2617)clearTimeout(tvReturnSettleTimerV2617);document.documentElement.classList.add('tvReturnStableV2617');
  const started=Date.now(),deadline=started+4200;let lastH=-1,lastTop=null,stable=0,foundOnce=false;
  const finish=()=>{if(tvReturnSettleTimerV2617)clearTimeout(tvReturnSettleTimerV2617);tvReturnSettleTimerV2617=null;setTimeout(()=>{document.documentElement.classList.remove('tvReturnStableV2617');tvReturnBusyV2617=false;window.__tvReturnHoldUntilV2617=Date.now()+1600;try{sessionStorage.removeItem(TV_RETURN_KEY_V268)}catch{}},220)};
  const settle=()=>{const cur=tvReturnReadV268();if(!cur){finish();return}const page=String(cur.page||'').replace(/[^a-z0-9_-]/gi,'');if(page&&document.querySelector('.pageTab.active')?.dataset?.page!==page){try{setPage(page,{force:true})}catch{document.querySelector(\`.pageTab[data-page="\${page}"]\`)?.click()}stable=0;tvReturnSettleTimerV2617=setTimeout(settle,100);return}
    const sym=String(cur.symbol||''),all=[...document.querySelectorAll('.rankCard,.testCard,.testMonitorCard,.actualTradeMonitorCard,.biasRow,.matrixCoin,.sg-candidate-card,.manual-card,.manual-shadow-history-row,.abc-sample-row,.actualTradeItemV2613')].filter(x=>x.offsetParent!==null);let card=cur.key?all.find(x=>stableElementKeyV2617(x)===cur.key)||null:null;if(!card&&sym)card=all.find(x=>String(x.querySelector?.('[data-tv-symbol]')?.dataset?.tvSymbol||'').toUpperCase()===sym)||null;const link=card?.querySelector?.('[data-tv-symbol]')||[...document.querySelectorAll('[data-tv-symbol]')].find(x=>String(x.dataset?.tvSymbol||'').toUpperCase()===sym&&x.offsetParent!==null)||null;
    const h=document.documentElement.scrollHeight,top=card?card.getBoundingClientRect().top:(link?link.getBoundingClientRect().top:null);if(card||link)foundOnce=true;if(foundOnce&&Math.abs(h-lastH)<=2&&top!=null&&lastTop!=null&&Math.abs(top-lastTop)<=1.5)stable++;else stable=0;lastH=h;lastTop=top;
    if((foundOnce&&stable>=2)||Date.now()>=deadline){tvReturnApplyV268();finish();return}tvReturnSettleTimerV2617=setTimeout(settle,100)};
  requestAnimationFrame(()=>requestAnimationFrame(settle));
}
`;
  s=replaceRange(s,'function tvReturnRestoreV268(){',"\ndocument.addEventListener('click',e=>",restore,'TV restore');

  // Auto refresh must not mutate the visible page while finger scrolling or while returning from TV.
  s=s.replace("setInterval(()=>{if(Number(window.__tvReturnHoldUntilV2611||0)>Date.now())return;const active=document.querySelector('.pageTab.active')?.dataset?.page;if(active==='today'){void refreshMarketFlow(false);void refreshDailyBrief(false)}else if(active==='flow')void refreshMarketFlow(false);else if(active==='ideas')void refreshRankedIdeas(false);else if(active==='test'||active==='monitor')void refreshTestSignals(false);else if(active==='performance')void refreshPerformance(false)},8_000);","setInterval(()=>{if(!uiAutoRefreshAllowedV2617())return;const active=document.querySelector('.pageTab.active')?.dataset?.page;if(active==='today'){void refreshMarketFlow(false);void refreshDailyBrief(false)}else if(active==='flow')void refreshMarketFlow(false);else if(active==='ideas')void refreshRankedIdeas(false);else if(active==='test'||active==='monitor')void refreshTestSignals(false);else if(active==='performance')void refreshPerformance(false)},8_000);");

  // Wrap the two largest list renderers: skip identical redraws and preserve one anchor on meaningful updates.
  if(s.includes('function renderRankedIdeas(')&&s.includes('async function refreshRankedIdeas(')){
    s=s.replace('function renderRankedIdeas(','function renderRankedIdeasBaseV2617(');
    const idx=s.indexOf('async function refreshRankedIdeas(');
    const wrapper=`function renderRankedIdeas(d){const sig=rankSignatureV2617(d),grid=$('recGrid'),root=document.querySelector('.pageTab.active')?.dataset?.page==='ideas'?(document.querySelector('.page.active')||grid):grid;if(sig===lastRankSigV2617&&grid?.children?.length){rankedIdeasState=d;return}const a=captureViewportAnchorV2617(root);lastRankSigV2617=sig;renderRankedIdeasBaseV2617(d);restoreViewportAnchorV2617(root,a)}\n`;
    s=s.slice(0,idx)+wrapper+s.slice(idx);
  }
  if(s.includes('function renderTestSignals(')&&s.includes('async function refreshTestSignals(')){
    s=s.replace('function renderTestSignals(','function renderTestSignalsBaseV2617(');
    const idx=s.indexOf('async function refreshTestSignals(');
    const wrapper=`function renderTestSignals(d){const sig=testSignatureV2617(d),grid=$('testGrid'),root=document.querySelector('.page.active')||grid;if(sig===lastTestSigV2617&&grid?.children?.length){testSignalsState=d;testSignalsFetchedAt=Date.now();return}const a=captureViewportAnchorV2617(root);lastTestSigV2617=sig;renderTestSignalsBaseV2617(d);restoreViewportAnchorV2617(root,a)}\n`;
    s=s.slice(0,idx)+wrapper+s.slice(idx);
  }

  s=`// ${MARKER}\n${s}`;return save(f,before,s);
}

function patchManual(){
  const f=must('public','manual-mode-ui.js'),before=fs.readFileSync(f,'utf8');let s=before;if(s.includes(MARKER))return false;
  s=s.replace(/function restoreManualAnchor\(box,a\)\{[^\n]*\}/,"function restoreManualAnchor(box,a){if(!a||window.uiMotionHeldV2617?.())return;requestAnimationFrame(()=>requestAnimationFrame(()=>{if(window.uiMotionHeldV2617?.())return;let el=a.external&&a.node?.isConnected?a.node:(a.id?box.querySelector(`[data-id=\\\"${CSS.escape(a.id)}\\\"]`):null);if(!el&&!a.external)el=a.kind==='rank'?box.querySelector('.manual-rank-history'):a.kind==='settings'?box.querySelector('.manual-settings'):a.kind==='grades'?box.querySelector('.manual-grade-summary'):box.querySelector('.manual-head');if(!el)return;const delta=el.getBoundingClientRect().top-a.top;if(Math.abs(delta)>8&&Math.abs(delta)<window.innerHeight*1.5)window.scrollBy({top:delta,left:0,behavior:'auto'})}))}");
  s=s.replace("async function refresh(force=false){if(state.busy)return;","async function refresh(force=false){if(state.busy)return;if(!force&&window.uiAutoRefreshAllowedV2617&& !window.uiAutoRefreshAllowedV2617())return;");
  s=`// ${MARKER}\n${s}`;return save(f,before,s);
}

function patchGrowth(){
  const f=path.join(__dirname,'public','system-growth.js');if(!fs.existsSync(f))return false;const before=fs.readFileSync(f,'utf8');let s=before;if(s.includes(MARKER))return false;
  s=s.replace(/function restoreGrowthAnchor\(root,a\)\{[^\n]*\}/,"function restoreGrowthAnchor(root,a){if(!a||window.uiMotionHeldV2617?.())return;requestAnimationFrame(()=>requestAnimationFrame(()=>{if(window.uiMotionHeldV2617?.())return;let el=a.external&&a.node?.isConnected?a.node:null;if(!el&&a.key)for(const x of root.querySelectorAll('[data-sg-detail-key],[data-sg-candidate],#sgAbcArena,#sgCandidateSection,#sgLessonSection,.sg-explore,.sg-attributes-layout,.sg-stage,.sg-core-card')){if(detailKey(x)===a.key||x.id===a.key){el=x;break}}if(!el)return;const delta=el.getBoundingClientRect().top-a.top;if(Math.abs(delta)>8&&Math.abs(delta)<window.innerHeight*1.5)window.scrollBy({top:delta,left:0,behavior:'auto'})}))}");
  s=`// ${MARKER}\n${s}`;return save(f,before,s);
}

function patchHub(){
  const f=must('public','actual-trade-hub-v2613.js'),before=fs.readFileSync(f,'utf8');let s=before;if(s.includes(MARKER))return false;
  if(s.includes("let records=[],busy=false,lastFetch=0,timer=null,quickCtx=null;"))s=s.replace("let records=[],busy=false,lastFetch=0,timer=null,quickCtx=null;","let records=[],busy=false,lastFetch=0,timer=null,quickCtx=null,lastHubShapeSigV2617='';");
  else if(!s.includes('lastHubShapeSigV2617'))throw new Error('[v2617] hub state anchor missing');
  const a=s.indexOf('function renderHub(){'),b=a>=0?s.indexOf('\nasync function refreshTrades',a):-1;if(a<0||b<0)throw new Error('[v2617] hub render range missing');
  const fn=`function renderHub(){const root=ensureHub();if(!root)return;const a=activeRows();if(hubHidden()){lastHubShapeSigV2617='HIDDEN:'+a.map(x=>x.id).join('|');const existing=root.querySelector('.actualHubHiddenV2616');if(existing){const n=existing.querySelector('b');if(n)n.textContent=String(a.length);return}root.innerHTML=\`<div class="actualHubHiddenV2616"><button type="button" class="actualHubReopenV2616" data-v2616-hub-toggle>已建倉 OFF <b>\${a.length}</b></button></div>\`;return}const oldFold=root.querySelector('.actualTradeFoldV2613'),foldOpen=oldFold?.open===true,openIds=new Set([...root.querySelectorAll('.actualTradeItemV2613[open]')].map(x=>x.dataset.actualId)),shape=JSON.stringify(a.map(x=>[x.id,x.status,x.resultAt,x.tp1,x.tp2,x.sp1,x.sp2,x.margin,x.leverage,x.quantity,x.revisionCount]));if(shape===lastHubShapeSigV2617&&oldFold){const count=oldFold.querySelector(':scope>summary em');if(count)count.textContent=String(a.length);for(const x of a){const card=root.querySelector(\`[data-actual-id="\${CSS.escape(String(x.id))}"]\`);if(!card)continue;const cur=card.querySelector('.actualTradeGridV2613>div:first-child b'),pWrap=card.querySelector('.actualTradeItemPnlV2613'),p=pWrap?.querySelector('b'),pnl=livePnl(x);if(cur)cur.textContent=px(x.lastPrice);if(p)p.textContent=pnlText(pnl);if(pWrap){pWrap.classList.remove('flat','profit','loss');pWrap.classList.add(pnlClass(pnl))}}return}lastHubShapeSigV2617=shape;root.innerHTML=\`<button type="button" class="actualHubHideV2616" data-v2616-hub-toggle aria-label="關閉已建倉區塊">已建倉 ON</button><details class="actualTradeFoldV2613"><summary><div><span>ACTUAL POSITIONS</span><b>已建倉</b><small>預設收合 · ON/OFF 可整區關閉 · 結案後立即移出</small></div><em>\${a.length}</em><i>⌄</i></summary><div class="actualTradeFoldBodyV2613">\${a.length?a.map(tradeCard).join(''):'<div class="actualTradeEmptyV2613">目前沒有追蹤中的實際建倉。</div>'}<small class="actualTradeBackstageNoteV2613">結案只移出前台；後台績效／歷史仍保留。</small></div></details>\`;const fold=root.querySelector('.actualTradeFoldV2613');if(fold)fold.open=foldOpen;root.querySelectorAll('.actualTradeItemV2613').forEach(x=>{if(openIds.has(x.dataset.actualId))x.open=true})}`;
  s=s.slice(0,a)+fn+s.slice(b);
  s=`// ${MARKER}\n${s}`;return save(f,before,s);
}

function patchIndex(){
  const f=must('public','index.html'),before=fs.readFileSync(f,'utf8');let h=before;if(h.includes('UI_STABILITY_V2617 styles'))return false;
  h=h.replace(/\/app\.js\?v=[^"']+/g,'/app.js?v=102617').replace(/\/manual-mode-ui\.js\?v=[^"']+/g,'/manual-mode-ui.js?v=sg2617').replace(/\/actual-trade-hub-v2613\.js\?v=[^"']+/g,'/actual-trade-hub-v2613.js?v=sg2617').replace(/\/system-growth\.js\?v=[^"']+/g,'/system-growth.js?v=sg2617');
  const css=`<style id="v2617-stability">/* UI_STABILITY_V2617 styles */html.tvReturnStableV2617{scroll-behavior:auto!important}html.tvReturnStableV2617 *,html.tvReturnStableV2617 *::before,html.tvReturnStableV2617 *::after{scroll-behavior:auto!important;transition:none!important;animation:none!important}html.tvReturnStableV2617{overflow-anchor:none!important}</style>`;
  h=h.replace('</head>',`${css}\n</head>`);fs.writeFileSync(f,h,'utf8');return true;
}

export function patchUiStabilityV2617(){const files={app:patchApp(),manual:patchManual(),growth:patchGrowth(),hub:patchHub(),index:patchIndex()};return{changed:Object.values(files).some(Boolean),files,marker:MARKER}}
if(import.meta.url===`file://${process.argv[1]}`)console.log(patchUiStabilityV2617());
