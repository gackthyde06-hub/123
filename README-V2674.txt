V2.6.74 — 候選標題排版修正

只修你截圖中的候選標題區，不動後端、不動候選邏輯、不動通知、不動 Shadow。

修正：
- 所有文字先分配自己的版面，不再和按鈕/箭頭搶空間。
- Shadow 學習後的手動候選：置中，可自然換行。
- 第一行：深析 → 候選池 → 安全
- 第二行：A/B → 候選
- 兩行資料改成純文字，不再有舊 CSS 的膠囊框把字擠住。
- 所有資料文字水平置中。
- 重新整理按鈕獨立一欄。
- 展開箭頭獨立一欄。
- 寬度不夠時直接換第二行；430px 以下直接把統計區移到下一列。
- 不縮成難看的小字、不允許文字蓋到任何物件。

部署：
1. 新增 candidate-header-layout-v2674-patch.mjs
2. 用 ZIP 內 prepare-ui.mjs 覆蓋 repo 根目錄同名檔
其他檔案不要動。
